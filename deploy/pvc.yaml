---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omni-repository-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Gi
  storageClassName: ssd

